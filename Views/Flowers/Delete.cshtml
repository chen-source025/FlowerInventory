@model FlowerInventory.Models.Flower

@{
    ViewData["Title"] = "刪除花卉";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-trash text-danger"></i> 刪除花卉</h2>
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> 返回清單
        </a>
    </div>

    <div class="card shadow">
        <div class="card-header bg-danger text-white">
            <h5 class="card-title mb-0">確認刪除</h5>
        </div>
        <div class="card-body">
            <h5 class="text-danger">您確定要刪除這個花卉嗎？此操作無法復原！</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-3">花名</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">品種</dt>
                <dd class="col-sm-9">@(Model.Variety ?? "無")</dd>

                <dt class="col-sm-3">分類</dt>
                <dd class="col-sm-9">@Model.Category</dd>

                <dt class="col-sm-3">ABC分類</dt>
                <dd class="col-sm-9">@Model.ABCClass</dd>

                <dt class="col-sm-3">保存天數</dt>
                <dd class="col-sm-9">@Model.ShelfLifeDays 天</dd>

                <dt class="col-sm-3">單價</dt>
                <dd class="col-sm-9">@(Model.Price?.ToString("C0") ?? "未設定")</dd>
            </dl>

            @if (Model.Batches?.Any() == true || Model.Transactions?.Any() == true)
            {
                <div class="alert alert-warning">
                    <h6><i class="fas fa-exclamation-triangle"></i> 警告</h6>
                    <p class="mb-0">
                        此花卉已有相關的批次或交易紀錄，無法刪除。
                        如需刪除，請先移除所有相關資料。
                    </p>
                </div>
                
                <div class="form-group text-center">
                    <a href="@Url.Action("Index")" class="btn btn-primary btn-lg">返回清單</a>
                </div>
            }
            else
            {
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />
                    <div class="form-group text-center">
                        <button type="submit" class="btn btn-danger btn-lg">
                            <i class="fas fa-check"></i> 確認刪除
                        </button>
                        <a href="@Url.Action("Index")" class="btn btn-secondary btn-lg">取消</a>
                    </div>
                </form>
            }
        </div>
    </div>
</div>